# Test Plan

Unit: known CAN IDs, scaling, offset, bit extraction and unknown IDs.
Integration: SocketCAN reception and CSV generation.
Fault: unknown ID, short DLC, receive errors and log-storage failures.

Do not claim OEM compatibility or measured bus performance without target-specific validation.